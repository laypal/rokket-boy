.env
.env.*
node_modules/
__pycache__/
*.pyc
.DS_Store
graphify-out/

dist/
playwright-report/
test-results/

# OpenCode local logs (observability plugin)
.opencode/logs/

# Playwright MCP browser artefacts (screenshots/snapshots from QA sessions)
.playwright-mcp/

# Claude Code worktrees (parallel subagent trees)
.claude/worktrees/

# superpowers brainstorm companion (mockups + screenshots, session-scoped)
.superpowers/

# TOOL.1/TOOL.2 previews (sprite PNGs, WAVs) — never committed
scratch/

# Sprite Studio (sprite-maker) workspace scaffold — local tool state, never committed
.sprite-studio/

# Sprite Studio workspace outputs — chars.ts holds the imported truth; PNGs regenerate via npm run sprite
/assets/
/exports/
